language: ruby
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
      - firefox

rvm:
  - ruby-head
  - 2.6
  - 2.5
  - 2.4
  - 2.3

script:
  - npm install -g markdownlint-cli
  - markdownlint Changelog.md
  - bundle exec rspec
