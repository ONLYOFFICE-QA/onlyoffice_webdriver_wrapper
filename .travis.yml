sudo: required
language: ruby
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
      - firefox

rvm:
  - ruby-head
  - 2.5.1
  - 2.4.4
  - 2.3.7

matrix:
  allow_failures:
  - rvm: ruby-head

bundler_args: "--without development"
script:
- bundle exec rspec
